{% extends 'base.html.twig' %}

{% block title %}Degree{% endblock %}

{% block body %}
    <h1>Degree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ degree.id }}</td>
            </tr>
            <tr>
                <th>DegreeTitle</th>
                <td>{{ degree.degreeTitle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" style="color: white;" href="{{ path('app_degree_index') }}">Back to list</a>

    <a class="btn btn-primary" href="{{ path('app_degree_edit', {'id': degree.id}) }}">Edit</a>
    <br><br>


    {{ include('degree/_delete_form.html.twig') }}
{% endblock %}
